const Renderer = struct {
    fn init() Renderer {
        return {

        };
    }
};

const Engine = struct {
    fn init() Engine {
        return {

        };
    }

    fn deinit(_: *Engine) void {

    }
};